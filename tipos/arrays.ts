const numeros: number[] = [1,2,3,4,5,6,7,8,9];

const villanos = ['Duende Verde','Megamente','Thanos'];

numeros.push(10,11);

villanos.forEach( v => (v.toUpperCase()))

console.log(numeros);
console.log(villanos);